Đầu tiên:
Cách 1 :Mở terminal(hoặc git bash) trỏ vào đồ án: 

cd C:/Users/NgocBinh/DoAnTichHop (thay bằng đường dẫn ở máy mình)
*(Git Bash dùng dấu / thay vì \)

Cách 2: Bấm chuột phải vào thư mục đồ án chọn "open git bash here" 

Để tải về code mới nhất:
git pull

Để tải lên code vừa làm:
git add . 
(enter)
git commit -m "ghi chú bất kì"
(enter)
git push
(enter)
